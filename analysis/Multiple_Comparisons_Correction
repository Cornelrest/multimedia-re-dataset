# lstlisting, language=Python, caption=Multiple Comparisons Correction, label=lst:correction
from statsmodels.stats.multitest import multipletests

# Original p-values for primary outcomes
p_values = [0.001, 0.01, 0.001, 0.001]  # Completeness, Precision, Recall, Satisfaction

# Bonferroni correction
rejected, p_corrected, _, _ = multipletests(
    p_values, 
    alpha=0.05, 
    method='bonferroni'
)

print("Corrected p-values:", p_corrected)
# Results: [0.004, 0.04, 0.004, 0.004] - all remain significant
